
const Heading = ({ title }) => {
    return (
        <div className="bg-slate-600 text-center">
            <h1 className="text-5xl font-semibold text-white p-5">{title}</h1>
        </div>
    );
};

export default Heading;